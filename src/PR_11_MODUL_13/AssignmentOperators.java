package PR_11_MODUL_13;

public class AssignmentOperators {
    public static void main(String[] args) {
        // Deklarasikan variabel integer a dengan nilai 10
        int a = 10;

        // Tambahkan 5 ke a menggunakan operator penugasan +=
        a += 5; // a sekarang menjadi 15

        // Kurangi 3 dari a menggunakan operator penugasan -=
        a -= 3; // a sekarang menjadi 12

        // Kalikan a dengan 2 menggunakan operator penugasan *=
        a *= 2; // a sekarang menjadi 24

        // Bagi a dengan 3 menggunakan operator penugasan /=
        a /= 3; // a sekarang menjadi 8

        // Mencari sisa dari a dibagi 2 menggunakan operator penugasan %=
        a %= 2; // a sekarang menjadi 0

        // Menampilkan hasil akhir
        System.out.println("Nilai akhir a: " + a);
    }
}

/*Penjelasan Program:
Deklarasi dan Inisialisasi: Variabel a dideklarasikan dengan nilai awal 10.
Penggunaan Operator Penugasan:
a += 5; menambahkan 5 ke a (sekarang a menjadi 15).
a -= 3; mengurangi 3 dari a (sekarang a menjadi 12).
a *= 2; mengalikan a dengan 2 (sekarang a menjadi 24).
a /= 3; membagi a dengan 3 (sekarang a menjadi 8).
a %= 2; mencari sisa dari a dibagi 2 (sekarang a menjadi 0).
Output: Program menampilkan nilai akhir dari a.
Silakan jalankan program ini dalam lingkungan Java Anda untuk melihat hasilnya!*/